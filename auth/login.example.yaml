# login.yaml - Example Configuration
# Copy this file to login.yaml and update with your actual credentials
credentials:
  usernames:
    admin:
      email: admin@exemplo.com # Substitua pelo email real
      name: Admin User
      password: '$2b$12$EXAMPLE_HASH_REPLACE_WITH_REAL_BCRYPT_HASH' # Senha hasheada para admin
      client_id: "ADMIN001" # ID de cliente associado
      roles: ["admin"]
    user:
      email: user@exemplo.com # Substitua pelo email real
      name: Regular User
      password: '$2b$12$EXAMPLE_HASH_REPLACE_WITH_REAL_BCRYPT_HASH' # Senha hasheada para user
      client_id: "CLIENTE001"
      roles: ["user"]
    gestor_master:
      email: gestor@empresa-exemplo.com.br
      name: EMPRESA EXEMPLO LTDA
      password: '$2b$12$EXAMPLE_HASH_REPLACE_WITH_REAL_BCRYPT_HASH'
      client_id: "EXEMPLO001"
      roles: ["admin", "user"]
cookie:
  expiry_days: 30
  key: 'REPLACE_WITH_STRONG_RANDOM_KEY_MINIMUM_32_CHARACTERS' # Chave forte e aleatória
  name: 'auditoria360_cookie'

# A seção preauthorized foi removida pois não é mais usada pelo authenticator