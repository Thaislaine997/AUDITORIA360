fastapi
uvicorn

# Conexão com Banco de Dados (Neon)
sqlalchemy
psycopg2-binary

# Análise de Dados (Substituto do BigQuery)
duckdb
pandas

# Armazenamento de Arquivos (Cloudflare R2)
boto3

# AI/ML Dependencies
openai>=1.0.0
python-dotenv>=1.0.0

# OCR (Substituto do Document AI)
paddleocr
paddlepaddle

# Orquestração de ML
prefect

# Autenticação e Utilitários
python-jose[cryptography]
passlib[bcrypt]
pydantic[email]

# Dashboard Frontend
streamlit
plotly
streamlit-authenticator

# MCP Integration Dependencies
pydantic>=2.0.0
pyyaml>=6.0
asyncio-mqtt>=0.11.1
websockets>=11.0
aiohttp>=3.8.0
jsonschema>=4.0.0

# Testing Dependencies
pytest>=8.0.0
pytest-asyncio>=0.21.0

# Missing Dependencies (Identified during diagnosis)
python-multipart
playwright>=1.30.0
scikit-learn>=1.3.0
redis>=4.0.0

# Security Dependencies for input validation
bleach>=6.0.0

# Communication Gateway Dependencies
twilio>=9.0.0